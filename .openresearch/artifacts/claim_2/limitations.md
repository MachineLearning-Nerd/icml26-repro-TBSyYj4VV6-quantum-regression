# Claim 2 limitations and deviations

The experiment simulates the distribution produced by the sampling stage; it
does not implement the quantum leverage-score subroutine, measure quantum
queries, or certify the asymptotic runtime. The hard finite matrices and
horizons were chosen independently of the claimed runtime formula, but finite
success cannot prove the universal theorem. No valid counterexample was found.
